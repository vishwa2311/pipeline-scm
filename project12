pipeline {
    agent any
    options { timestamps() }

    stages {
        stage('Checkout main repo') {
            steps {
                echo "Checking out main repository..."
                checkout([
                    $class: 'GitSCM',
                    branches: [[name: '*/main']],
                    userRemoteConfigs: [[
                        url: 'https://github.com/YOUR_USER/jenkins-pipeline-demo.git'
                    ]]
                ])
                bat '@echo off & echo ✅ Main repo checked out & dir'
            }
        }

        stage('Checkout lib repo') {
            steps {
                dir('libs') {
                    echo "Checking out library repository into 'libs' folder..."
                    checkout([
                        $class: 'GitSCM',
                        branches: [[name: '*/main']],
                        userRemoteConfigs: [[
                            url: 'https://github.com/YOUR_USER/some-shared-lib.git'
                        ]]
                    ])
                    bat '@echo off & echo ✅ Lib repo checked out & dir'
                }
            }
        }

        stage('Combined Build') {
            steps {
                bat '@echo off & echo 🏗️ Running combined build across repos... & powershell -Command "Start-Sleep -Seconds 1"'
            }
        }
    }

    post {
        always {
            echo "✅ Multi-repo pipeline finished successfully."
        }
    }
}
