pipeline {
    agent any
    stages {
        stage('Checkout Git') {
            steps {
                echo "Pipeline started! Git repo fetched automatically by Jenkins."
            }
        }
    }
}
